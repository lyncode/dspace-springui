		<div class="navbar navbar-fixed-top">
    		<div class="navbar-inner">
        		<div class="container">
            		<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                		<span class="icon-bar"></span>
                		<span class="icon-bar"></span>
                		<span class="icon-bar"></span>
		            </a>
        		    <a class="brand sobrio" href="{{ path '/' }}">DSpace SpringUI</a>
					<div class="nav-collapse collapse">
						<div class="pull-right">
							<!--
							<ul class="nav">
						  		{% if user.person.repositories %}
						  		<li class="dropdown">
							  		<a class="dropdown-toggle" data-toggle="dropdown" href="#"><i class="icon-tasks"></i> {{ 'Repositories' | trans }} <b class="caret"></b></a>
							  		<ul class="dropdown-menu">
							  			{% for rep in user.person.repositories %}
								 			<li><a href="{{ concat ('/repository/', rep.id, '/view') | path }}">{{ rep.name }}</a></li>
										{% endfor %}
							  		</ul>
							  	</li>
						  		{% endif %}
						  		
							</ul>
							-->
							<a class="btn btn-inverse" href="{{ path '/logout' }}"><i class="icon-off icon-white"></i> {{ 'Logout' | trans }}</a>
						</div>
					</div>
        		</div>
    		</div>
		</div>